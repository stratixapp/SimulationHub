# assets/js

Reserved for phase-specific script modules as later phases are built
(e.g. `resume.js`, `payroll.js`). Phase 1 logic currently lives in the
root `script.js`. As more phases are added, each new phase's module
will be split into its own file here and loaded from `index.html`.
