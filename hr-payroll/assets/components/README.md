# assets/components

Reserved for shared, reusable UI partials (e.g. a standalone data-table
component, a shared modal component) once multiple phases need the
same building blocks beyond what's already generalized in script.js
(Toast, Confirm, Popovers).
