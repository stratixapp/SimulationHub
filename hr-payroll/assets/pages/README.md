# assets/pages

Reserved for full-page HTML partials per phase, if the app grows large
enough that inlining every phase's markup into index.html becomes
unwieldy (e.g. loaded via fetch() or a template `<template>` tag).
