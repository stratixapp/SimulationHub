# assets/css

Reserved for phase-specific stylesheets as later phases are built
(e.g. `resume.css`, `payroll.css`). Phase 1 styles currently live in
the root `style.css` — this folder will hold split-out stylesheets
once the app has enough phases to warrant it, per the "one phase at
a time, modular" build rule.
