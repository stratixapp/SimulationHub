# assets/data

Reserved for reference/seed data files. Not used yet — dropdown option
lists (departments, qualifications) are currently defined directly in
script.js so the app works reliably when opened straight from the file
system (fetch() of local JSON can be blocked by the browser under the
file:// protocol).
